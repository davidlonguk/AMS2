<?php

/**
 * @file
 * Test file for AMS development.
 * 
 */

class AssetManagementTestCase extends DrupalWebTestCase {
  protected $privileged_user;

  public static function getInfo() {
    return array(
      'name' => 'Asset Management Module Tests',
      'description' => 'For Automated Testing of Asset Management Module features',
      'group' => 'AMS',
    );
  }

	public function setUp() {
    // Enable any modules required for the test. This should be an array of
    // module names.
    parent::setUp(array('assetmanagement'));
    // Create and log in our privileged user.
    $this->privileged_user = $this->drupalCreateUser(array(
      'administer ams',
      'edit ams',
      ));
    $this->drupalLogin($this->privileged_user);
  }
  
  
  // Test Admin can change state of Editor Imports
  public function testAssetManagementEditorImportToggle() {
  	
	//$this->drupalGet($path) 
	//check variable exists
	//function variable_get($assetmanagement_editor_import, $default = NULL) {
  	global $conf;

        $valid = isset($conf[$assetmanagement_editor_import]) ? $conf[$assetmanagement_editor_import] : $default;
        $this->assertTrue($valid, 'Editor Import is Enabled.');

    }
		
 
}
